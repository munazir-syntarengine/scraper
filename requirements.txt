# Brand Website Scraper Demo — Python dependencies
# Install:  pip install -r requirements.txt
# Then:     playwright install chromium   (downloads the headless browser)

# Web framework + ASGI server
fastapi>=0.115
uvicorn[standard]>=0.34

# Headless render — launches Chromium to fetch + render the target page.
# Pinned to match the Docker base image (mcr.microsoft.com/playwright/python).
playwright==1.60.0

# HTML -> text cleaner (boilerplate stripping + flattening)
beautifulsoup4>=4.12
lxml>=5.3
